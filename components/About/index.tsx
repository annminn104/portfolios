import styles from './index.module.scss';
import React from 'react';

const About: React.FC = () => (
  <React.Fragment>
    <section className='section' id='about'>
      Component About is working file!
    </section>
  </React.Fragment>
);

export default About;
